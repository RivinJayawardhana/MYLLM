# ---- Core LLM stack (model, tokenizer, math) ----
torch>=2.0
tiktoken>=0.5
numpy>=1.24,<2.0        # tensorflow is not yet compatible with numpy 2.x

# ---- GPT-2 weight download & checkpoint loading ----
tensorflow>=2.13        # only needed to read the original GPT-2 checkpoints
requests>=2.28
tqdm>=4.65

# ---- Web UI (Streamlit apps) ----
streamlit>=1.30
